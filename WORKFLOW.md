# HomeRelay ハッカソン作業手順

この手順は、Codexに実装を任せながら、2時間でデモを完成させるための進行表です。

## 1. 開始前に確認する

1. Codexで次のフォルダだけを開く。

   ```text
   <path-to-homerelay-fast-hacks>
   ```

2. フォルダ名が `HomeRelay_Codex_Handoff_2026-08-27` であることを確認する。
3. CareRelayのフォルダ、リポジトリ、資料、環境変数は開かない。
4. 実在する利用者・家族・ヘルパーの情報を入力しない。
5. スマートフォンとPCの2台をデモ用に用意する。

## 2. Codexに最初に渡す文

新しいCodexセッションで、このまま貼り付ける。

```text
このリポジトリでHomeRelayのMVPを自律的に実装してください。

最初にAGENTS.md、CODEX_START_HERE.md、README.md、WORKFLOW.md、docs/hackathon-build/内の全ファイルを最後まで読んでください。その後、docs/hackathon-build/checklist.mdを上から順に実行してください。

CareRelayには絶対に触れないでください。CareRelayのコード、資料、リポジトリ、データベース、API、環境変数、Google Driveを読まない・検索しない・変更しない・コピーしないでください。このリポジトリの絶対パスを最初に確認し、作業範囲をここだけに限定してください。

2時間のハッカソンなので、温かいUI、アプリ内カメラ、音声入力、AI確認、リアルタイム共有、必要品、Qdrantの順で進めてください。主導線が2回連続で成功するまで、Neo4j、CodeRabbit、Datadogの追加作業は始めないでください。

外部サービスの認証情報がない場合もUI作業を止めず、明確に表示した架空データのデモフォールバックを用意してください。ただし、ライブ接続が成功したとは報告しないでください。各項目の検証結果をchecklist.mdとbuild-notes.mdへ記録してください。

まず、作業ディレクトリ、CareRelay非接触、最初の3項目、足りない認証情報だけを短く報告し、そのまま実装を開始してください。
```

## 3. Codexが最初に報告する内容

次の4点がそろっていることを確認する。

- 作業場所が `HomeRelay_Codex_Handoff_2026-08-27` になっている。
- CareRelayを開かないと明言している。
- 最初の作業がUI・カメラ・音声の順になっている。
- 不足している認証情報だけが列挙されている。

違うフォルダが表示された場合は、実装を止める。

## 4. 外部サービスの準備

主導線のUI開発と並行して、HomeRelay専用の認証情報だけを用意する。

### 必須

- OpenAI API key
- HomeRelay専用Supabase project
  - Project URL
  - Publishable key
  - Server-only secret key
- Qdrant
  - Cluster URL
  - API key
  - Collection name: `homerelay_entries`

### 後回し

- Neo4j
- Datadog
- CodeRabbit
- HackerSquad提供環境

認証情報は `.env.local` にだけ設定する。チャット、スクリーンショット、Gitへ貼らない。

## 5. 2時間の進行

| 経過 | Codexの作業 | あなたの確認 |
|---|---|---|
| 0〜15分 | 独立確認、Next.js作成、温かいホームUI | 文言と色が希望どおりか |
| 15〜40分 | アプリ内カメラ | スマホで撮影・撮り直しできるか |
| 40〜65分 | 音声録音、AI下書き、確認画面 | 話すだけで短い文章になるか |
| 65〜90分 | Supabase保存、別端末へ即時反映 | スマホ入力がPCへ届くか |
| 90〜105分 | `買います`、`買いました` | 担当と状態が両端末で変わるか |
| 105〜113分 | Qdrant類似検索・重複警告 | 意味の近い過去記録が出るか |
| 113〜120分 | 通しテスト2回、デモ固定 | 60秒デモが止まらないか |

## 6. UI確認で見る場所

- 見出しが `今日の様子` になっている。
- `写真を撮る`、`話す`、`これでOK`、`次の人へ` が大きく見える。
- 文字が純黒や極端な太字になっていない。
- 説明文が長くない。
- 写真が中心に見える。
- 役割が `ご家族`、`ご親族`、`訪問ヘルパー` と文字で表示される。
- `訪問看護師` が画面やデモデータに存在しない。
- 必要品が `買います`、`買いました` だけになっている。

## 7. スマートフォン実機確認

カメラとマイクは、PC上の見た目だけでは合格にしない。

1. HTTPSのURLをスマートフォンで開く。
2. カメラを許可する。
3. アプリの画面内で背面カメラが起動することを確認する。
4. 撮影、撮り直し、採用を行う。
5. マイクを許可し、短く話す。
6. AI下書きを確認する。
7. 確認前にPCへ表示されていないことを確認する。
8. `次の人へ` の後だけPCへ表示されることを確認する。

## 8. 60秒デモ

### スマートフォン側

1. `写真を撮る`
2. 架空の食事を撮影
3. `話す`
4. 次を話す。

   ```text
   昼食は半分ほど。水分を用意しました。トイレットペーパーが少ないです。
   ```

5. AI下書きを確認
6. `これでOK`
7. `次の人へ`

### PC側

1. 写真と申し送りが数秒以内に現れる。
2. `見ました` → `私がやります` → `できました` と進める。
3. トイレットペーパーの `買います` を押す。
4. `買いました` にする。
5. Qdrantの関連記録または重複警告を見せる。

### 締めの言葉

> HomeRelayは、監視ではありません。介護する人が、写真と声だけで、次の人へ温かくバトンを渡すWebアプリです。

## 9. 時間が足りないときの切り方

次の順で削る。主導線は削らない。

1. Datadog
2. CodeRabbitの詳細対応
3. Neo4j
4. Qdrantの関連履歴表示を1種類だけにする
5. 装飾アニメーション

削ってはいけないもの:

- 写真
- 音声
- AI確認
- 確認後だけ共有
- 別端末への反映
- `見ました` / `私がやります` / `できました`
- `買います` / `買いました`
- 温かく簡単なUI

## 10. 完成報告でCodexに確認する

Codexの最終報告に次がなければ追記させる。

- 動作した機能
- デモ用フォールバックの箇所
- 実行した検証コマンドと結果
- まだ必要な環境変数
- スマートフォンで未確認の項目
- 60秒デモ2回の結果
- CareRelayへ変更がないこと

## 11. 提出前の判断

機能数ではなく、次の一連の流れが止まらず動くことを優先する。

```text
写真を撮る → 話す → AIが短く整える → 本人が確認する
→ 次の人へ → 家族画面へ届く → 見ました → 私がやります
→ できました → 買います → 買いました
```
