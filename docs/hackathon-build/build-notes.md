# Build Notes

## Handoff decisions

- 2026-08-27: User directed that Codex, not this assistant turn, will implement the application.
- Created a completely independent empty Git repository for HomeRelay.
- CareRelay was not read, changed, copied, or referenced as an implementation source.
- Autonomous speed-run selected because the hackathon build window is short.
- Core demo is protected by a stop rule: no Neo4j, CodeRabbit, or Datadog work before the main flow passes twice.
- Exact current wording is `見ました` / `私がやります` / `できました` and `買います` / `買いました`; no alternate purchase, refill, or delivery wording.
- Visiting nurses were explicitly removed. Shared content is a family-facing handoff summary, not an official provider record.

## Build log

Codex should append one dated entry after every completed checklist item with:

- Files changed
- Verification commands and results
- Live integration or fallback status
- Any checklist adaptation and reason

### 2026-08-27 — Checklist 1 complete: isolation and scaffold

- Files changed: initialized the HomeRelay-local Git repository; added pinned Next.js 16.3.3 / React 19.2.8 / Tailwind 4.3.3 scaffold, TypeScript, ESLint, Vitest, App Router shell, server-only integration status, synthetic fixtures, lockfile, and explicit demo banner. Updated stale HomeRelay working-path text in `WORKFLOW.md`.
- Verification: the working path resolved to the HomeRelay repository root; `npm run lint` passed; `npm run typecheck` passed; `npm run build` passed; dev `GET /` and `GET /api/status` returned 200; status returned demo for OpenAI, Supabase, and Qdrant; `npm audit --omit=dev --audit-level=high` reported 0 vulnerabilities.
- Isolation: repository source/config/dependency scan found no implementation or package reference to any external project path. Policy documents retain only the required non-contact boundary wording.
- Live/fallback: `.env.local` and vendor credentials are absent. The app starts in a clearly labeled, synthetic-only demo mode and does not issue vendor requests.
- Adaptation: latest published ESLint 10 and TypeScript 7 exceeded the peer range of the pinned Next.js lint toolchain, so compatible pinned versions ESLint 9.39.5 and TypeScript 6.0.3 were selected. A broken `lucide-react` 1.34.0 publish was replaced with local typed SVG icons after both type and production-build failures.

### 2026-08-27 — Checklist 2 complete: warm responsive shell

- Files changed: added the warm `今日の様子` home feed, photo-first confirmed handoff card, the three allowed role badges, exact action and purchase wording, large labeled controls, responsive record-callout shell, loading/error/not-found states, local typed icons, and a generated synthetic meal photo at `public/demo/synthetic-meal.png`.
- Synthetic image: generated with OpenAI's built-in image generation using an explicitly fictional overhead Japanese lunch scene with no people, names, text, logos, or other personal information. It is demo media only.
- Verification: `npm run lint`, `npm run typecheck`, and `npm run test:run` passed; Playwright + Chrome passed 4/4 checks across 390 px phone and 1280 px desktop layouts; axe found no serious or critical violations after the secondary-text contrast correction; both screenshots were visually inspected; source scan found no pure-black token, forbidden purchase/refill/delivery wording, or visiting-nurse role.
- Live/fallback: the shell uses synthetic fixtures and displays `合成デモモード`; no external service call is made. The generated photo is bundled locally so the UI remains reliable without credentials.
- Adaptation: `agent-browser` was not installed in the workspace, so the equivalent visual and accessibility verification was performed with pinned Playwright against the installed Chrome browser.

### 2026-08-27 — Checklist 3 implementation complete; manual phone check pending

- Files changed: added rear-camera-first `getUserMedia`, in-page preview, capture, retake, accept and cancel states; canvas JPEG re-encoding with a 1600 px long-edge cap; EXIF-removing fresh-canvas export; full MediaStreamTrack cleanup; and an `image/*` fallback shown only when the media API is unavailable.
- Automated verification: camera/image unit tests passed 6/6; the complete unit suite later passed 29/29; Playwright + Chrome synthetic-camera capture, retake, and accept passed at 390 px and 1280 px; production build passed. Visual inspection confirmed the accepted-photo screen.
- Privacy: temporary Object URLs are revoked, active tracks stop on capture/cancel/unmount and stale permission requests, and accepted photos remain client-side at this stage. No gallery-save API is used.
- Pending acceptance check: a real HTTPS smartphone permission/camera-indicator check is still required. This item remains unchecked; the user asked work to continue without interruption, so later checklist implementation proceeds while this hardware-only gate stays explicit.

### 2026-08-27 — Checklist 4 complete: voice recording and human-confirmed AI draft

- Files changed: added an audio-only MediaRecorder flow, permission/retry/cancel states, temporary-audio cleanup, size and MIME guarded `/api/draft` route, server-only OpenAI transcription and structured-output adapter, strict Zod schema, deterministic missing-credential fallback, editable draft form, and a local `これでOK` confirmation boundary.
- Verification: `npm run lint`, `npm run typecheck`, `npm run test:run` (7 files / 29 tests), `npm run test:e2e` (8/8 across phone and desktop), and `npm run build` passed. Route tests covered valid, empty, bad MIME, oversized, missing-key, invalid JSON/schema and safe upstream-error cases; component tests covered audio-only constraints, track cleanup, permission denial, retry, edit and confirmation.
- Privacy and boundary: raw audio and partial transcripts are not logged or returned to the browser; the in-memory Blob is discarded after processing/cancel/failure. Browser verification confirmed that no share endpoint is called before or after `これでOK` at this stage and the UI still says `まだ家族には共有されていません`.
- Live/fallback: `OPENAI_API_KEY` is absent and `HOMERELAY_DEMO_MODE=true`; therefore no OpenAI request was made or claimed. The UI explicitly labels `合成AI下書き（OpenAI未接続）`. The live server adapter and schema validation remain ready for credentials.

### 2026-08-27 — Emergency demo pivot: local two-tab MVP complete

- Scope change: with 20 minutes remaining, the user stopped cloud Supabase/Auth/RLS and all Neo4j, Datadog, CodeRabbit, HackerSquad and new-credential work. The app was deliberately narrowed to a synthetic, same-browser demo.
- Files changed: added a HomeRelay-specific localStorage store with BroadcastChannel plus `storage` event fan-out, confirmed-entry publication only after `次の人へ`, live family-tab feed updates, and interactive needed-item claim/completion state changes. Added a two-page browser E2E and the exact README demo script.
- Supabase boundary: CLI/config and an unverified migration draft were started, then stopped and removed before public release. Local Supabase did not start because port 54322 was already allocated; no other project/container was inspected or stopped, no cloud was contacted, and this work is not reported as complete or connected.
- Qdrant gate: `QDRANT_URL` and `QDRANT_API_KEY` were both safely checked as unset without printing values. Per the emergency instruction, Qdrant connection work was skipped and is not reported as used.
- Demo verification: `npm run test:e2e -- tests/e2e/demo-flow.spec.ts --repeat-each=2 --workers=1` passed four runs: phone viewport 8.1s and 7.7s, desktop/family viewport 8.6s and 7.3s. Each run covered photo, voice, synthetic AI draft, edit, human confirmation, share, family-tab appearance, purchase intent and purchase completion, with external HTTP(S) requests blocked and observed count 0.
- Limitations: persistence and synchronization are browser-local, not authenticated or cross-device. Real smartphone camera/microphone permission remains a manual check. External service integrations remain explicitly unconnected.

### 2026-08-27 — Formal-development restart audit and preserved checkpoint

- Checkpoint: the working emergency demo and all tracked changes are preserved on clean `main` at commit `3a8cb24` (`feat: complete HomeRelay hackathon MVP`). No reset, force overwrite, or removal of the working demo flow was performed.
- Reconciliation: Item 1 and 2 are complete; Item 3 is partial only because its HTTPS smartphone check remains; Item 4 is complete with an explicit missing-credential fallback; Item 5 and 8 are not started; Item 6, 7, 9, and 10 are partial. `checklist.md` now records these states against the actual files.
- Working checkpoint flow: in-page photo capture, audio-only recording, deterministic synthetic AI draft, editable human confirmation, share only after `次の人へ`, browser-local family-feed update, acknowledgement/action transitions, and purchase intent/completion.
- Emergency fallbacks retained: `lib/demo-relay.ts` provides localStorage/BroadcastChannel synchronization, and `lib/ai/openai-draft.ts` is gated by an explicit synthetic fallback when credentials or live mode are absent. Neither is described as cloud or vendor connectivity.
- Formal order: HomeRelay-only Supabase schema/Auth/RLS/Storage/Realtime; confirmed-only durable sharing; attributable action/purchase transitions; Qdrant; hardening and two-device verification; only then Neo4j, Datadog, CodeRabbit, and final publication.
- Missing connectivity: there is no `.env.local`. HomeRelay-only Supabase, OpenAI, Qdrant, Neo4j, and Datadog credentials are absent; GitHub CLI authorization is invalid; CodeRabbit has no connected GitHub App/PR. Local implementation continues without claiming any of these services are connected.
- Boundary: all reads and writes in this restart audit were confined to the HomeRelay repository and the selected build-tool instruction files; no external project path, data, credential, or service was accessed.

### 2026-08-28 — Checklist 5 complete: HomeRelay-only Supabase, Auth, Storage, and RLS

- Files changed: added one clean HomeRelay migration and synthetic seed, invite-only password Auth, Next.js 16 Proxy/session clients, private `handoff-photos` Storage policies, five household-scoped tables, explicit Data API grants, confirmed-only guarded RPCs, Realtime publication, and local verification scripts.
- Security model: the authenticated user ID is mapped to exactly one household member; household IDs and actor IDs are always derived from that session. Direct client writes are denied. Cross-household rows, mutations, Storage objects, and Realtime events are denied by RLS. No service-role key is used by the app.
- Verification: `npx supabase db reset --local` passed from an empty database; pgTAP passed 2 files / 50 assertions; the security and performance advisors reported no warnings; `verify:supabase:local` passed invited login, uninvited signup rejection, same-household access, foreign-household rejection, private Storage, and entry/photo referential integrity.
- Live/fallback: the local HomeRelay Supabase stack is actually connected and verified. No cloud Supabase URL/key was supplied, so no cloud project connection is claimed. The isolated localStorage/BroadcastChannel demo remains available only when explicitly selected.

### 2026-08-28 — Checklist 6 complete: confirmed-only durable share and Realtime

- Files changed: added the formal Relay interface, Supabase and demo adapters, authenticated `/api/entries`, image signature/size validation, private upload, SHA-256-bound idempotency, safe ambiguous-error compensation, signed-photo refresh, Realtime status handling, authoritative post-subscribe refresh, and auth-subject state reset.
- Verification: a phone-width helper browser and a separate authenticated desktop-family browser completed the entire flow twice consecutively in 24.3 seconds total. Both confirmed handoffs appeared through Supabase Realtime; no live failure fell back to local demo storage. The independent Realtime script also received household-filtered entry/item events and confirmed a foreign household received none.
- Reliability adaptation: the test server uses a dedicated live port to prevent reuse of a demo process; cold local Realtime is warmed before event timing; missing private photos render an isolated placeholder instead of failing the entire feed; camera retake performs one short transient-device retry.

### 2026-08-28 — Checklist 7 complete: attributable response and purchase states

- Files changed: added guarded acknowledgement, entry claim/completion, item purchase-intent/completion RPC calls and UI ownership gates. The final user-facing labels are recorded in the 2026-08-30 entry below; internal state and RPC names remain unchanged.
- Verification: pgTAP covered allowed transitions, duplicate idempotency, cross-household rejection and non-owner completion rejection. The two-client Realtime script observed `confirmed → claimed → done` and `needed → purchase_intent → purchased`; the cross-browser flow exercised the same buttons twice.
- Privacy: only operation IDs/statuses traverse these state calls; no photo, audio, name, or handoff body is logged by the application.

### 2026-08-28 — Items 5–7 checkpoint revalidation

- Database and access checks: `npx supabase test db` passed 2 files / 50 assertions; `npx supabase db lint --local --level warning` reported no schema errors; `verify:supabase:local` passed invite-only Auth, same/foreign-household RLS, guarded RPCs, private Storage, and entry/photo integrity; `verify:supabase:realtime` passed both Realtime state sequences and foreign-household non-delivery.
- Test isolation: the RLS assertions now prove every visible entry, item, and Storage path belongs to the signed-in household instead of assuming the database always contains exactly one synthetic row. This keeps the negative test valid after repeated verification runs.
- Browser checks: the synthetic demo suite passed 10 tests with 2 intentionally skipped live-only cases across phone and desktop projects. The HomeRelay local-Supabase flow then passed twice consecutively using an isolated phone-width helper context and desktop-family context; the two-run test completed in 46.2 seconds and each run enforced its own 60-second limit.
- Final checkpoint checks: `npm run verify` passed ESLint, TypeScript, 23 test files / 106 tests, and the Next.js production build. Playwright now uses a HomeRelay-local direct Next test server plus a loopback-only shutdown control so Windows test runs exit without orphaning the server.
- Scope truth: these results prove local Supabase connectivity and separate authenticated browser contexts. They do not prove a hosted Supabase project, HTTPS smartphone hardware, separate physical devices, OpenAI API, or Qdrant connectivity; those remain explicit pending gates.

### 2026-08-28 — Checklist 8 implementation complete; Qdrant live gate pending

- Files changed: added a server-only Qdrant Cloud Inference adapter, strict environment validation, deterministic UUIDv5 point IDs, HomeRelay collection bootstrap, live verification script, confirmed-share background indexing, related-candidate API, Supabase RLS revalidation, and the related-handoff/duplicate-item panel.
- Isolation and privacy: every vector query contains `household_id` and point type as `must` filters plus the current entry as `must_not`. Returned payloads are validated again, then candidate IDs are fetched through the signed-in Supabase client and rejected unless RLS returns a same-household row. Qdrant receives only human-confirmed summary text and needed-item names; it never receives photos, audio, member names, or caller-controlled household IDs.
- Reliability: indexing is registered with Next.js `after()` only after `share_handoff` succeeds. Missing credentials, invalid responses, timeouts, and vendor errors return an explicit unavailable result and never change the successful Supabase share. Demo mode makes no Qdrant request and labels its fixture-derived candidate `合成候補（Qdrant未接続）`.
- Verification: targeted Item 8 checks passed 8 files / 55 tests, covering household filters, current-entry exclusion, a three-result cap, malformed/foreign payload rejection, Supabase revalidation, purchased-item exclusion, failed indexing isolation, AbortController cleanup, and every UI state. The complete synthetic browser suite passed 10 tests with 2 expected live skips; the local-Supabase phone/desktop flow still passed twice consecutively in 53.2 seconds with Qdrant explicitly absent. Final `npm run verify` passed ESLint, TypeScript, 29 test files / 152 tests, and the production build. Bootstrap and verifier syntax checks passed.
- Live/fallback truth: `npm run verify:qdrant` returned `SKIP / 未接続` because explicit Supabase live mode plus `QDRANT_URL` and `QDRANT_API_KEY` were not available; it made no external request. Therefore Qdrant remains implemented but not connected or claimed as used. Live completion requires `npm run qdrant:bootstrap` followed by `npm run verify:qdrant` with HomeRelay-only credentials.

### 2026-08-28 — Checklist 9 complete locally: privacy, input limits, errors, and accessibility

- Files changed: added a shared streaming request reader for JSON/multipart routes; real-byte limits for actions, draft audio, and confirmed entries; CSP and browser security headers; no-runtime-console linting; dynamic-state axe checks; privacy audit; CI and PR templates; and a CodeRabbit review configuration.
- Input hardening: malformed or contradictory `Content-Length`, stream errors, and body-length mismatches return 400. Actual bytes are counted with or without a declared length; over-limit streams are cancelled and return 413. The focused reader/route set passed 49/49 tests.
- Privacy: the audit checks every publishable file, reachable Git history, approved synthetic media hashes, forbidden recordings/photos/logs, runtime content logging, a fresh production build, and browser-delivered bundles for secret values or server-only environment markers. It passed 160 files and 37 browser-delivered build files.
- Accessibility/security verification: phone and desktop Playwright checks include initial and dynamic camera/AI states, visible focus, labels, and HTTP header assertions. `npm audit --omit=dev --audit-level=high` reported 0 vulnerabilities; lint, typecheck, 42 unit files / 275 tests, and production build passed.

### 2026-08-28 — Optional sponsor adapters and truthful connection evidence

- Neo4j: added a server-only Query API projection for household/member roles, confirmed handoffs, response assignees, purchase assignees, and household-scoped item concepts. It sends identifiers, role/action/timestamps, and scoped hashes only—never photos, audio, names, handoff text, or item names. Five fixed uniqueness constraints, redirect rejection, read-back bootstrap, synthetic verifier, active-mode gate, and non-blocking failure behavior are implemented.
- Datadog: added server-only numeric metrics for API duration, AI duration, and error count with fixed low-cardinality route/outcome/mode tags. IDs, URLs, exception text, stack traces, photos, audio, names, handoff text, and item text are excluded. Submission is non-blocking and disabled before network access without valid live configuration.
- CodeRabbit: added schema-v2 configuration, HomeRelay privacy/RLS/accessibility review instructions, ESLint/gitleaks checks, CI, and a PR template. No GitHub App or PR bot review occurred, so CodeRabbit is configured but not claimed as used.
- Live truth: `npm run verify:sponsors` produced `SKIP / 未接続` for Qdrant, Neo4j bootstrap/verifier, and Datadog because their credentials are absent. No external sponsor request was made, and none is reported as live-used.

### 2026-08-28 — Cloud verifier, local credential hardening, and final local demo evidence

- Credential handling: the committed seed now assigns unreachable random hashes. Local admin verifiers accept only scoped `HOMERELAY_TEST_*` loopback values, rotate only fixed synthetic users with process-only passwords, and redact errors. Manual demo provisioning is a separate loopback-only command and must run after automated tests. No `.env.local` or populated vendor credential is present.
- Hosted verification at that checkpoint: added an explicit-opt-in HTTPS verifier for invite-only Auth, same-household sharing, foreign-household Data/RPC/Storage rejection, positive Realtime delivery, and foreign Realtime non-delivery. Its initial public-client-only version rejected loopback, service-role/secret keys, and missing inputs; the checkpoint result was `SKIP / non-connected` because no HomeRelay cloud project or invited accounts had yet been supplied. This historical result is superseded by the cloud audit below.
- Database evidence: pgTAP passed 2 files / 50 assertions; schema lint found no errors; Auth/Data/Storage/RLS verification passed; Realtime state transitions and foreign-household non-delivery passed.
- Demo evidence: the synthetic Playwright suite passed 12 tests on phone and desktop with 2 intentional live-only skips. The local-Supabase test then completed the photo→voice→AI draft→human confirmation→share→acknowledge→claim→done→purchase intent→purchased flow twice between isolated helper/family contexts; the test took 53.4 seconds and enforced each run below 60 seconds.
- Remaining gates at that checkpoint: this proved local Supabase and separate authenticated browser contexts, not a hosted project or two physical devices. The hosted Supabase status is superseded below; HTTPS smartphone camera/microphone permission, a separate physical PC, live Qdrant/Neo4j/Datadog, CodeRabbit PR review, and public GitHub push remain explicitly unverified.

### 2026-08-28 — HomeRelay cloud pre-publication safety audit

- Target confirmation: Git root, `main`, starting HEAD `a67a76af129f6764680d753272b94328cc1da43e`, no remote, linked ref `czfmqaeqamepntpsakbv`, and local/remote migration `20260827114534_homerelay_core` all matched before editing. The hosted project is `HomeRelay`, region `ap-northeast-1`, status `ACTIVE_HEALTHY`.
- Cloud schema read-back: RLS is enabled on all five public tables (`households`, `members`, `entries`, `needed_items`, `acknowledgements`); no RLS policy references `user_metadata`; `handoff-photos` is private; and `entries`, `needed_items`, and `acknowledgements` are in `supabase_realtime`. No migration, schema, bucket, Realtime, or Auth setting was changed.
- Verifier hardening: every Storage SDK response now checks `error`; 401 is accepted only after the synthetic session's Auth user and membership were revalidated, while structured 403/`42501` and the operation-specific Storage non-disclosure response for a separately proven existing object also count as expected denial. 429, 5xx, timeout, and transport failures take precedence over any accompanying denial marker and fail. PostgREST operations receive an AbortSignal, and Auth/Storage use a per-fetch timeout. Known IDs/paths are recorded before risky probes, and unexpected or response-ambiguous foreign INSERT/UPDATE probes are recovered with a server-only admin client. Cleanup is one pass in Storage → child-first Database → Auth order, checks every result, preserves both primary and cleanup failures, and repeats the final read-back after the request-timeout window without repeating deletion.
- Unit coverage at that earlier audit: the focused cloud-verifier suite passed 45 tests, including Storage create/delete failure, Data cleanup failure, 401/403/RLS denial, mixed denial/infrastructure markers, 429/5xx/network/timeout and AbortSignal delivery, unexpected foreign INSERT success and recovery, immediate/late residual failure, wrong Project URL, non-`.test` email refusal, deterministic cleanup order, and secret-safe logging. The final post-fix count is recorded below.
- Hosted acceptance: with process-only credentials and randomly generated `.test` users for this audit, private Storage SELECT/INSERT/UPDATE/DELETE scoping passed; same-household share/read passed; foreign SELECT, direct INSERT/UPDATE, guarded RPC, photo access, and Realtime delivery were denied; and the same household received the expected Realtime event. Direct Data API INSERT/UPDATE are denied at the missing authenticated write GRANT (`42501`), while household write RLS is directly exercised by Storage policies and membership enforcement is exercised through guarded RPCs.
- Cleanup evidence: every run began and ended with Auth users 0, every target public table 0 rows, and `handoff-photos` objects 0. No real user or real data was created. The credentials and passwords used for the live cloud connection were held only in process memory and were not printed, written to documentation, or committed; repository tests contain synthetic non-credential literals only.
- Historical blocker at this audit: Auth reported `disable_signup=false` and `mailer_autoconfirm=false`. The verifier therefore ended with `FAIL uninvited_signup_not_rejected` even though all membership, Storage, Data, RPC, Realtime, and cleanup checks passed. Auth configuration changes were prohibited in that audit; this result is superseded by the invite-only finalization below.
- Advisors: Security Advisors reports 6 WARN, all lint `0029` for the six authenticated guarded `SECURITY DEFINER` RPCs. They use fixed `search_path`, `auth.uid()`-derived membership, and revoked PUBLIC/anon execute, but the warnings remain unresolved pending explicit design acceptance. The initial Performance Advisors result was 14 INFO: 6 composite foreign keys without covering indexes and 8 indexes unused in the empty project. After the synthetic acceptance queries, the unused-index notices cleared; the current result is 6 INFO for the unindexed foreign keys. No schema change caused that reduction, and no advisor-driven migration was added in this task.
- Integration truth: Supabase is now live-connected and tested. OpenAI Codex was used for implementation and audit. OpenAI API, Qdrant, Neo4j, and Datadog remain live-unconnected; CodeRabbit is configured but unused because no correct HomeRelay GitHub repository/PR exists.
- Local verification at that earlier audit: `npm run lint` and `npm run typecheck` passed; Vitest passed 42/42 files and 306/306 tests; the Next.js 16.3.3 production build passed; and `npm run audit:privacy` passed 160 publishable files, reachable Git history, and 37 browser-delivered files with no credential pattern or server-secret marker. The final post-fix results are recorded below.

### 2026-08-28 — Invite-only Auth finalization and cloud re-acceptance

- Minimal Auth change: current official Supabase Auth configuration docs, Management API reference, changelog, CLI 2.116.0 version, and relevant `--help` output were checked. `config push` was rejected because it can push the full local `config.toml`. On exact project `czfmqaeqamepntpsakbv`, the Dashboard changed only “Allow new users to sign up” from ON to OFF. Public Auth settings changed from `disable_signup=false` to `true`; `mailer_autoconfirm=false` and Confirm email ON remain. All public Auth settings except `disable_signup` produced the same SHA-256 before and after: `e2308943ad6e80fd6f9b61d72cd13ab7f615194f037080b37db404c454a62318`. No URL, email, schema, migration, bucket, Realtime, or API-key setting changed.
- Invitation verification: an uninvited random `@homerelay.test` signup returned `signup_disabled` with no user/session. The server-only admin `generateLink({ type: "invite" })` path created a synthetic invite link and user without sending email, and a public client redeemed its hashed token with `verifyOtp({ type: "invite" })` to obtain that same user's authenticated session. The user was included in the same strict cleanup. This proves invite generation and redemption remain available without claiming SMTP delivery.
- Function audit and acceptance: all six lint `0029` WARNs are intentional authenticated `public.SECURITY DEFINER` RPCs. `share_handoff` atomically creates a confirmed handoff and needed items without accepting a household ID; `acknowledge_entry` confirms a current-household entry; `claim_entry` and `complete_entry` enforce current household and claimant; `claim_needed_item` and `complete_needed_item` do the same for purchase state. Every function has `search_path=''`; EXECUTE is held by `postgres` (owner), `authenticated`, and `service_role`, with none for PUBLIC/anon; and all SQL is fixed and fully qualified with no dynamic SQL or SQL-input interpolation. Each execution path calls `private.current_member_id()` and `private.current_household_id()`, which require non-null `auth.uid()` and map it through `members.auth_user_id`; target rows are constrained to that derived household. Live same-household calls to all five action RPCs succeeded and persisted the expected transitions. Live foreign-household calls to those RPCs were accepted as denied only for authenticated `403` or SQLSTATE `42501` responses, never a bare `401`; `share_handoff` has no foreign household ID input and validates its photo path against the derived household/member. service-role/secret values stayed in the one-time Node.js process and were absent from browser configuration, logs, files, and Git. The six WARNs are accepted without schema changes.
- Performance acceptance: the final Advisor result remains six INFO notices for composite household foreign keys without covering indexes: two on `acknowledgements`, two on `entries`, and two on `needed_items`. The retained data count is zero and the notices affect neither functionality nor authorization. No index or migration was added; future work will measure representative queries and inspect `EXPLAIN (ANALYZE, BUFFERS)` before deciding.
- Hosted re-acceptance: preflight was empty; general signup denial, admin invite-link generation/redemption, synthetic family/helper sign-in, RLS 5/5, same-household share/read and all five action transitions, foreign SELECT and all ID-targeted guarded RPC denials, private Storage cross-household SELECT/INSERT/UPDATE/DELETE denials, same-household Realtime receipt, and foreign non-delivery all passed. Cleanup succeeded once. Independent SQL after the verifier found Auth users 0; `households`, `members`, `entries`, `needed_items`, and `acknowledgements` each 0; `handoff-photos` objects 0; the bucket remained private; and the three expected Realtime tables remained published.
- Integration truth: Supabase is live-connected and fully passes this cloud acceptance scope. OpenAI Codex was used for requirements, implementation, audit, and verification. OpenAI API, Qdrant, Neo4j, and Datadog remain live-unconnected; CodeRabbit remains configured but unused/unconnected; HackerSquad remains unused/unconnected. None is reported as live-used.
- Final local verification: focused cloud-verifier 47/47 and full Vitest 42/42 files with 308/308 tests passed. Full lint, typecheck, Next.js 16.3.3 production build, `npm run audit:privacy`, an added-file/diff/environment credential-pattern scan, and `git diff --check` passed. Privacy audit covered 160 publishable files, reachable Git history, and 37 browser-delivered build files; no private media, credential pattern, runtime-content log, or server-secret marker was found.

### 2026-08-30 — OpenAI API live verification and final wording alignment

- Target and cost preflight: the key row was read back inside the exact HomeRelay OpenAI Project `proj_QWYQHfrfBwh5aIAHbCVXb3hP`. The organization balance was $18.18, HomeRelay Project display spend was $0.00, and its $1 hard limit was unchanged before calling the API.
- Files changed: added a loopback-only, explicit opt-in OpenAI verifier protected by a one-time token; bound the SDK to `OPENAI_PROJECT_ID`; forced `logLevel=off` and `maxRetries=0`; added minimal reasoning, a bounded output budget, empty-transcript rejection, fixed content-free error classes, and focused tests. Normal paid calls now require Supabase live mode and an invited authenticated session. The UI auto-stops at 30 seconds; the route validates that declared duration plus an actual 2 MiB byte ceiling and applies an in-runtime member/household rate and concurrency guard before the vendor call. Media duration is not independently decoded server-side, and the OpenAI Project hard limit remains the durable cost boundary. The verifier exception is rejected in production and unless loopback host, opt-in flag, and constant-time token comparison all pass. `.env.example` documents variable names only; `.env.local` remains untracked and Git-ignored.
- Live result: only a 6.97-second local ja-JP synthetic WAV with no personal information was used. The first route request returned a safe 502 without response-body/vendor-detail logging. After usage read-back and adapter hardening, the next and final route request passed `gpt-4o-mini-transcribe` transcription and `gpt-5-mini` strict structured drafting end to end. No further OpenAI call was made.
- Cost read-back: the final Dashboard aggregate was 3 requests, 588 input tokens and 419 output tokens (1,007 total). Organization balance remained $18.18 and Project display spend remained $0.00 with the $1 hard limit unchanged. At the 2026-08-30 official model rates, assigning all 419 output tokens to the higher of the two output rates gives a conservative cost ceiling below $0.0024; the Dashboard does not expose enough decimal places to claim an exact $0 cost.
- Privacy and confirmation: API key, response body, transcript, draft body, names, photos, and real care data were not logged or documented. The temporary WAV was deleted and independently read back as 0 files. The verifier checks lunch, water, and tissue independently and waits longer than the route maximum so a client timeout cannot leave an unobserved server call. On AI failure the UI does not parse the error body or synthesize success; it opens four empty manual fields and still requires `これでOK` plus `次の人へ`. Phone/desktop E2E proved no `/api/entries` call or demo-storage write before confirmation and explicit sharing. Focused tests also prove one-time 30-second UI auto-stop with track release/submission, authenticated success, unauthenticated rejection, production/flag-off verifier rejection, actual-byte/declared-duration bounds, and fourth-attempt rate rejection before any paid call.
- Wording alignment: visible action labels now follow the final product instruction: `見ました` → `私がやります` → `できました`, and `買います` → `買いました`. Internal status values and guarded Supabase RPC names are unchanged.
- Qdrant verifier hardening: live cleanup deletion is no longer best-effort; deletion failure or any residual point fails the verifier, a possibly partial upsert still receives one cleanup pass, and fixed redacted CLI output is covered by 9 files / 68 focused tests. Vendor scripts load ignored `.env.local` values through Node's `--env-file-if-exists` while process values can still override them. Qdrant credentials remain absent, so no live request was made and no use is claimed.
- Test isolation: synthetic Playwright always starts a fresh server with forced demo mode. Supabase live E2E also isolates OpenAI, Qdrant, Neo4j, and Datadog at the server configuration boundary and fulfills only a strict synthetic draft inside the helper browser. It therefore exercises Supabase without vendor charges, optional-vendor writes, or cleanup obligations. The final action wording and all five transitions passed twice on phone width and twice on desktop; the AI-failure manual path passed on both widths.
- Final local verification for this checkpoint: lint and typecheck passed; Vitest passed 46/46 files and 347/347 tests; Playwright passed 14/14 synthetic tests with 2 intentional Supabase live-only skips; the updated demo flow additionally passed four consecutive project runs (phone twice and desktop twice); production build passed; privacy audit passed across 166 publishable files, reachable Git history, and 37 browser-delivered build files; dedicated secret scan, dependency audit, and `git diff --check` passed.
- Next external gate: the current-process Supabase cloud verifier truthfully returned `SKIP` because its one-time `HOMERELAY_CLOUD_*` credentials are no longer present; this does not replace or invalidate the recorded 2026-08-28 hosted acceptance. `QDRANT_*`, `NEO4J_*`, and `DD_*` are unset, and their console pages require user sign-in/sign-up. GitHub CLI still reports the saved `rin0908` token invalid. No cluster, database, Datadog organization, GitHub repository, deployment, or submission was created in this checkpoint.
- Integration truth: OpenAI API is now live-connected and verified. Supabase remains live-connected and previously accepted. Qdrant, Neo4j, Datadog, CodeRabbit, and HackerSquad remain unchanged and are not claimed as live-used.

### 2026-08-30 — Qdrant and Neo4j live verification; remaining sponsor boundaries

- Files changed: added 2026 Aura username/database connection compatibility and focused Neo4j tests, strengthened the Datadog verifier to fixed non-personal synthetic counters/duration, and aligned the four current-status documents. The existing Qdrant bootstrap/verifier required no additional code change for its live pass.
- Qdrant verification: Qdrant Cloud Free `homerelay-qdrant` was read back as Healthy. Bootstrap succeeded; Cloud Inference returned the related synthetic handoff and needed-item result with the foreign household excluded; every verification point was deleted and read back as 0.
- Neo4j verification: AuraDB Free `homerelay-graph` was read back as Running. Five constraints passed read-back; Home and foreign synthetic graphs were written with parameters; the expected family/relative/helper/handoff/assignment/purchase relationships were read back; the Home filter returned 0 foreign graph rows; and both households read back node and relationship counts of 0 after cleanup.
- Privacy and cleanup: only synthetic verification data was used. Qdrant points and both Neo4j household graphs were removed and independently read back as empty. No Qdrant/Neo4j credential value, endpoint, username, instance identifier, handoff body, or personal information was added to these four documents.
- Datadog boundary: the verifier now submits only fixed-tag synthetic success/failure counts and a numeric duration, requires the documented intake acceptance, and does not read vendor response details. AP1 Japan registration was attempted with two different email addresses; both verification-code send/resend flows returned the Datadog-side message `Unknown error` / 「不明なエラー」. No API key was created or saved and no live request or UI read-back occurred, so Datadog remains unconnected and unused. The same registration action is stopped and may be retried only once after the other completion work.
- HackerSquad boundary: builder login succeeded, but the target event was Archived and exposed neither a submission button nor a project path. No project was created or submitted, and authentication alone is not reported as use.
- Checklist adaptation: Item 8 is now complete and live verified. Item 10 still awaits an HTTPS deployment, a real smartphone plus separate PC, Datadog, PRIVATE GitHub/CodeRabbit review, and an available HackerSquad submission path.

### 2026-08-30 — Datadog registration block and private-release preflight

- Datadog remains unconnected and unused after the AP1 Japan verification-code send/resend flow returned the vendor-side unknown error for two different email addresses. No key was created or saved, no metrics were submitted, and the same registration flow is paused until at most one final retry after the other work.
- CodeRabbit configuration now disables automatic reviews and review caching and opts out of retained Knowledge Base features. A review must be requested explicitly on the single HomeRelay PR; configuration alone remains unused evidence.
- Current release verification: lint and typecheck passed; Vitest passed 49/49 files with 374/374 tests; Playwright passed 14/14 synthetic tests with 2 intentional live-only skips; the Next.js 16.3.3 production build passed; production dependency audit found 0 vulnerabilities.
- Privacy and publication preflight: `.env.local` remained ignored and untracked; its actual secret values were loaded only into the privacy-audit process and compared without output. The audit passed 170 publishable files, reachable Git history, and 27 browser-delivered files. Tracked secret env files, forbidden build/log/media paths, foreign-project file paths, and secret-value bundle matches were all 0; `git diff --check` passed.

### 2026-09-01 — iPhone app-like capture and immediate guarded actions

- Physical evidence before this code change: the Vercel HTTPS Preview was opened on an actual iPhone helper session and a separate Windows family session. Camera, microphone, OpenAI draft, pre-confirmation privacy, confirmed Supabase share, Realtime display, all three handoff actions, both purchase actions, Qdrant result, and Neo4j projection completed live. The measured end-to-end run was about 121 seconds, so it proves the real flow but not the 60-second-twice goal.
- UX diagnosis: the visible URL was Safari chrome rather than a camera/audio redirect; Web code cannot hide it in a normal browser tab. The home capture CTA and record-page camera-start button also duplicated the start action. Each Windows action previously waited for its own network response and full-list read-back, making the controls feel unresponsive.
- Files changed: added a scoped standalone manifest, Apple Web App metadata, generated HomeRelay icons, and an iPhone-only home-screen hint. The primary CTA now opens `/record?camera=1`; the camera starts once after navigation while preserving the OS permission prompt, shutter, retake, photo acceptance, explicit microphone start, and human confirmation boundary.
- Action behavior: handoff and purchase controls apply an immediate optimistic state, allow the next valid transition, batch rapid actions behind one authenticated route call, serialize overlapping batches, and reconcile once from Supabase. Failure restores the pre-batch state and keeps a visible warning. Page-hide/visibility-hide flushes pending actions with keepalive. The server accepts at most 10 schema-validated actions, authenticates once, executes only existing household-guarded RPCs in order, stops after a failure, and preserves both Neo4j purchase events; Supabase remains the source of truth.
- Verification: lint and typecheck passed; Vitest passed 56/56 files and 433/433 tests; Playwright passed 16 synthetic tests with 2 intentional live-only skips, including phone/desktop one-tap camera E2E; Next.js 16.3.3 production build generated `/manifest.webmanifest`, `/icon`, and `/apple-icon`; dependency audit found 0 vulnerabilities; `git diff --check` passed. Privacy audit passed 190 publishable files, reachable Git history, and 41 browser-delivered files with no private media, credential pattern, runtime content log, or server-secret marker.
- External truth: no OpenAI, Supabase, Qdrant, Neo4j, Datadog, CodeRabbit, or HackerSquad live call was made for this local UX checkpoint. Existing verified statuses are unchanged. CodeRabbit is connected and triggered only on the correct PRIVATE HomeRelay PR but no review ran under the Free private-repository limit; Datadog remains unconnected/unused and HackerSquad remains Archived/unsubmitted. The next gate is push-triggered Preview deployment followed by iPhone Home Screen + Windows physical regression; the 60-second-twice goal is not yet claimed.

### 2026-09-01 — Mobile new-entry access follow-up

- Physical finding: on the updated Preview, the actual iPhone Home Screen app opened the in-page camera after one tap. The native iOS camera prompt still requires each user to grant permission once per device/Web App; HomeRelay does not and cannot silently grant that OS permission.
- UX finding: the desktop two-column DOM order placed the detailed `新しく伝える` aside after the entire feed on a one-column phone layout, so a household with many entries had to scroll past every card before starting a new handoff.
- Fix: the detailed aside remains on desktop only. Phone layouts now keep a large `新しく伝える` CTA fixed above the iOS safe area at every scroll position, with bottom page padding so the final content remains reachable. It continues to open `/record?camera=1`, so the one-tap auto-camera behavior and confirmation boundary are unchanged.
- Verification: lint and typecheck pass; Vitest passes 56/56 files and 434/434 tests; Playwright passes 16/16 synthetic tests with the 2 live-only cases intentionally skipped, including phone/desktop one-tap camera, mobile fixed-position/overflow, and axe serious/critical checks; the production build and privacy audit pass. External integration status is unchanged; this UI change makes no vendor call.

### 2026-09-02 — Physical iPhone/Windows acceptance, timed runs, and final cleanup

- Devices and deployment: tested only on the protected PRIVATE Vercel Preview, using an actual iPhone Home Screen helper session and a separate Windows Chrome family session. This was not a Production or public deployment.
- Physical flow: the fixed `新しく伝える` CTA remained reachable without scrolling; one tap opened the in-page camera. Camera, microphone, OpenAI live draft, pre-confirmation non-sharing, confirmed Supabase persistence, Realtime arrival on Windows within 10 seconds, all three handoff actions, both purchase actions, and the Qdrant related-candidate display passed. The user rated the physical action response「普通（許容）」.
- Consecutive timing: a Windows stopwatch measured 60 seconds and 56 seconds from the iPhone fixed-CTA tap through the Windows `買いました` update. Login, Vercel protection, Home Screen installation, and first OS permissions were completed before timing. The 56-second synthetic run used a safe photo that did not semantically match the spoken needed item; it is valid path/timing evidence, not evidence that photo content and draft meaning matched.
- Pre-cleanup server read-back: Supabase returned the latest entry as `done`, with `confirmed` / `claimed` / `done` and a purchased needed item; the foreign household returned no entry. Qdrant contained 28 fixture points, indexed the latest handoff and one needed item, and returned no foreign-household point. Neo4j returned the latest handoff as `done` with 7 relationships and no foreign-household handoff.
- Cleanup: the fixed HomeRelay fixture ledger scoped deletion to two synthetic households, three `.test` users, validated private-Storage paths, household-filtered Qdrant points, and HomeRelay-labelled Neo4j nodes. Cleanup ran once. Both the immediate read-back and the second read-back after settling returned Auth users 0; `households`, `members`, `entries`, `needed_items`, `acknowledgements` 0; Storage objects 0; Qdrant fixture points 0; Neo4j fixture nodes 0 and relationships 0.
- Temporary credentials and media: the three synthetic fixture password entries were removed from ignored `.env.local`, the one-time iPhone login QR and ignored synthetic WAV/hosted-test results were deleted, the temporary Supabase server secret existed only in the cleanup process, and the fixture ledger was removed after both zero read-backs.
- External truth: Datadog is still unconnected and unused; CodeRabbit is connected and triggered on the correct PRIVATE PR but no review ran; HackerSquad is Archived and unsubmitted. The PR remains unmerged, and no Production deployment or public release was performed.
- Final local verification: lint and typecheck passed; the focused fixed-CTA/fixture suite passed 22/22 tests; Vitest passed 56/56 files with 434/434 tests; Playwright passed 16/16 synthetic tests with 2 intentional live-only skips after fixture cleanup; the Next.js 16.3.3 production build passed; privacy and reachable-history secret scanning passed 190 publishable files and 41 browser-delivered build files; the production dependency audit found 0 vulnerabilities; and `git diff --check` passed.

### 2026-09-02 — Sanitized PUBLIC source and CodeRabbit full review

- Publication boundary: reachable Git author/committer Gmail metadata was rewritten only to GitHub noreply while preserving messages, dates, and trees. The sanitized 11-commit history was pushed without the seven old private SHA identifiers to the new PUBLIC `rin0908/homerelay-web-fast-hacks-2026-public`; the original private repository remained private. The public privacy audit found no CareRelay/Badger files, private media, credential pattern, runtime content log, or server-secret marker.
- Review evidence: CodeRabbit was connected only to the new PUBLIC repository. Manual `@coderabbitai full review` on PR #1 completed against `b9e56a5813b0116d08eb88e992fede66c9c2f336`, returned a successful review-completed check, and produced 23 actionable inline comments. The PR is still OPEN and unmerged.
- Disposition: 20 comments were corrected in code, tests, or documents. One fake-timer comment was stale because the suite already restores real timers in `afterEach`. One proposal to disable vendor isolation in `test:e2e:live` was rejected after repository-level review because that suite is intentionally Supabase-only and removing isolation could spend money or leave Qdrant/Neo4j/Datadog data; hosted vendor E2E uses a separate config against Preview. The in-memory OpenAI request guard limitation is accepted as a single-runtime defense layer, not a distributed hard quota; the HomeRelay Project hard spending limit remains the final cost boundary.
- Main fixes: batched action partial-success counts now reconcile the successful prefix and perform one final authoritative Supabase read after every queued action settles; zero/invalid audio duration returns 400; device magic links preserve path/query, consume once under Strict Mode, use a shared Preview-only gate, and time out each Auth/membership stage with late-session cleanup; live verifiers load ignored local env safely, accept canonical Datadog aliases consistently, retry only transient hosted vendor read-back errors, and expand negative/cleanup/runtime tests.
- Verification: lint and typecheck passed; Vitest passed 58/58 files with 457/457 tests; Playwright passed 16/16 synthetic phone/desktop tests with 2 expected live-only skips after fixture cleanup; Next.js 16.3.3 production build passed; privacy/reachable-history/bundle secret audit passed 194 publishable files and 41 browser-delivered files; production dependency audit found 0 vulnerabilities; and `git diff --check` passed.
- Remaining gate: push the reviewed fixes to the public PR branch, obtain CodeRabbit incremental review, and record its result. The pre-review PRIVATE Preview still holds the physically accepted 60-second/56-second build; these review fixes are not yet deployed. Datadog remains unconnected/unused, HackerSquad remains Archived/unsubmitted, and no Production deployment occurred.

### 2026-09-02 — CodeRabbit incremental fixes and session-bound release candidate

- Review follow-up: CodeRabbit's first incremental review on `32db7d9…` returned 4 actionable inline comments plus 1 outside-diff action timeout finding. All five were verified against the code and fixed: Node is now constrained to 22.9.0+, live flags are normalized, Qdrant/Neo4j body-read failures remain retryable without hiding validation failures, the late Auth cleanup test waits on a condition, and `/api/actions` has bounded transport and response-body timeouts.
- Session hardening: latest `f996dc6…` adds a HomeRelay-only HttpOnly session guard and server-side session fingerprint. Password login, device login, and logout serialize Auth mutations with one shared Web Lock; signed-out state fails closed; delayed cookie resurrection, stale sessions, deleted membership, and browser/server identity mismatches do not render the private HomeRelay DOM. Unsupported Web Locks or disabled JavaScript now show a concrete safe browser message instead of enabling an unlocked mutation path.
- Action uncertainty: a timeout or unreadable response body is treated as indeterminate rather than a definite rollback. Dependent queued actions are rejected until an authoritative Supabase `list()` read-back clears the latch, preserving Supabase as the source of truth and avoiding duplicate non-idempotent assumptions.
- Baseline verification at `f996dc6…`: `npm run verify:release` passed lint, typecheck, 68/68 Vitest files with 549/549 tests, 16/16 synthetic Playwright tests with 2 expected live-only skips after fixture cleanup, and the Next.js 16.3.3 production build. Privacy/reachable-history/browser-bundle audit passed 213 publishable files and 43 browser-delivered files; the independent full-history secret scan found 0 matches; production dependency audit found 0 vulnerabilities; and `git diff --check` passed.
- Evidence boundary: the physical 60-second and 56-second runs, Supabase/Qdrant/Neo4j live read-backs, and all-zero cleanup remain valid evidence from the earlier PRIVATE Preview. `f996dc6…` has not been Preview-deployed or physically retested, so those results are not attributed to this latest HEAD. Browser-level two-context fault injection for delayed `Set-Cookie`, Web Locks ordering, and BroadcastChannel interaction remains an integration-evidence gap; the relevant ordering is covered by focused unit tests.
- CodeRabbit second incremental review: the review for `f996dc6…` completed successfully and returned 9 actionable comments. The current response candidate adds bounded device-session/logout fetches, complete fail-closed session state resets, a longer fallback poll, all-pending action rejection, generation-safe recovery for already queued batches, missing negative session tests, one shared live-E2E flag parser, single-project live fixture provisioning, authoritative completion read-back, and scoped DB/Storage cleanup with zero read-back. The integrated focused suite passed 103/103 tests, and `npm run verify:release` passed lint, typecheck, 68/68 Vitest files with 580/580 tests, 16/16 synthetic Playwright tests with 2 expected live-only skips, Next.js 16.3.3 production build, and a 215-publishable/43-browser-file privacy audit. Production dependency audit found 0 vulnerabilities and `git diff --check` passed. The candidate has not yet been pushed or re-reviewed.
- Current external state: PR #1 remains OPEN and unmerged; no Production deployment occurred. The current candidate is not Preview-deployed or physically retested, so the earlier 60-second/56-second evidence is not attributed to it. Datadog remains unconnected/unused after the AP1 registration error, HackerSquad remains Archived/unsubmitted, and the previously cleaned synthetic Supabase/Qdrant/Neo4j fixtures remain recorded as 0 without recreating them.
